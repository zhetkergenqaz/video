{
  "_comment": "Вставить в .claude/settings.json проекта (или ~/.claude/settings.json). Хук получает JSON вызова на stdin; exit 2 = блок, stderr уходит агенту.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"$CLAUDE_PROJECT_DIR\"/scripts/hooks/guard-reads.py"
          }
        ]
      }
    ]
  }
}

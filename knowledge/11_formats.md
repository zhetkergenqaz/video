# Три формата рилсов — что спрашивать и как собирать

Перед любым монтажом агент спрашивает Александра одно: **какой формат?** Ответ определяет раскладку, ритм и
источники. Не угадывать. Записать ответ в `DIRECTION.md` первой строкой.

| | Экспертный рилс | Разговорный рилс с вставками | Подкаст-рилс |
|---|---|---|---|
| Источник | съёмка спикера 9:16 + записи экрана | съёмка спикера 9:16, без экранов | отрывок 4K-подкаста, кроп по спикеру 1215×2160 |
| Спикер | H на всю нижнюю половину ↔ T портрет, смены ≤ 10 с | H ↔ T, смены ≤ 8 с | **всегда нижняя половина** `0,960 · 1080×960`, без рамки |
| Верх кадра | карточки, экраны, кинетика по словам | кинетика, знаки, штампы, рукопись: 1 объект на мысль | анимации, схемы, футажи, цитаты собеседников |
| Субтитры | 868 в H, 1272 в T | те же | **центр 960 ровно на шве**: половина плашки на видео, половина на канвасе (Александр 03.09) |
| Ритм | сцена на каждую мысль, 2–5 с | вставка каждые 3–5 с, не длиннее 2 с без действия | смысловое событие каждые 2–3 с, кадр не стоит дольше 2 с |
| Переходы | 3–5 стоковых/маркер на структурных склейках | маркер-штрихи, без стока | маркер + 1–2 стоковых |
| Звук | ≤ 3 SFX | ≤ 3 SFX | ≤ 3 SFX, речь подкаста без музыки |
| CTA | по бренд-плейбуку | по бренд-плейбуку | ссылка на полный подкаст, шапка |
| Стиль-образец | КАНВАС, PRISM, ORBIT, TRACE (`patterns/README.md`) | СЦЕНЫ, СТЕКЛО | ПОДКАСТ (`studio/src/styles/podcast.tsx`) |

## Разговорный рилс с вставками — как удерживать внимание без экранов

Речь без графики держит 3–4 с, дальше отваливаются. Правило вставок: каждое смысловое существительное или
число получает объект наверху (штамп, чип со знаком, число с pop, рукописная подпись, короткий список
терминалом), объект живёт 1.5–2 с и уходит, следующий появляется на следующем слове-триггере (03_rules §1).
Между вставками спикер меняет положение H ↔ T, а фон меняет радиал. В T-фазах верх занимает кинетический
стек по словам; в H-фазах — карточка с одной мыслью. Три слоя движения: слово субтитра (караоке),
объект наверху, смена положения спикера. Никаких зумов на спикере; динамика только сменой состояний.

## Подкаст-рилс — уникальная система

1. Отрывок выбирается по описи `reference/clips-index.md` (балл ≥ 60) или новой серии `videos/podcast-clips`.
2. Спикер: вертикальный кроп подкаста по говорящему (Александр — центр кадра 4K), окно H. Другие участники
   появляются вставкой сверху как «реплика собеседника»: кадр 16:9 в экран-карточке с адресной строкой имени.
3. Верхняя половина — смысловой канвас: схема мысли (SVG рисуется штрихом), цифры счётчиком, цитата
   собеседника карточкой, футаж из самого подкаста (у него уже есть авторские вставки) в экран-карточке целиком.
4. Заголовок отрывка — первая фраза-хук из описи, Manrope 800, стоит первые 2 с над канвасом, потом уходит.
5. Субтитры на шве 960, караоке; финальные 3 с — плашка «полный выпуск — в шапке» + знаки площадок.
6. Готовый отрывок = отдельный проект: `bash scripts/new-video.sh podcast-<slug> reels <отрывок.mp4>`, стиль ПОДКАСТ
   (`studio/src/styles/podcast.tsx`); вертикальный кроп спикера — `scripts/podcast-speaker.py`.

### Спикер подкаста — сборка под окно 1080×960 (`scripts/podcast-speaker.py`)

В подкасте есть монтажные склейки между общим и крупным планом. На крупном голова занимает 45 % высоты
колонки, и никакой сдвиг внутри окна не спасает: подбородок уходит под описание Instagram. Поэтому спикер
собирается по сегментам: скрипт ищет склейки (`scene > 0.12`), классифицирует план по яркости верхне-центральной
зоны колонки (общий: тёмный занавес ≈ 22, крупный: лоб и волосы ≥ 40), общий план режет кропом 1215×1080 из
колонки спикера со смещением 0.28 высоты (голова у верха окна), крупный — кропом 2430×2160 вокруг лица, и
склеивает в один файл 1080×960. В сториборде: `native 1080x960`, `H.pos 50% 50%`, `moves []`, `format: podcast`
(V24 не требует смен положения — динамику даёт верхний канвас). Планы можно задать вручную: `--segs "0:w,7.53:c,…"`.
Колонки 4K-кадра: Николай x 40, Александр x 1312, Дилором x 2585 (ширина 1215).

### Что взято из скиллов Эли в подкаст-формат
Счётчик набегает за время произнесения числа; список альтернатив — терминалом строка за строкой; сравнение —
горизонтальные бары с подписями JetBrains Mono; вопрос — рукописным заголовком с печатным подстрочником; карточка
не стоит пустой дольше 2 с (подпись-нота заполняет паузу до счётчика); CTA — чип в верхнем канвасе, не поверх спикера.

export const meta = {
  name: 'reels-animation-patterns-2026',
  description: 'Собрать каталог приёмов анимации для вертикальных рилсов и правила выбора приёма под смысл',
  phases: [
    { title: 'Разведка', detail: 'пять срезов: типографика, инфографика, монтаж подкастов, готовый код, правила выбора' },
    { title: 'Проверка', detail: 'отсеять выдуманное и нереализуемое' },
  ],
}

const LENSES = [
  { key: 'kinetic', prompt: `Собери приёмы КИНЕТИЧЕСКОЙ ТИПОГРАФИКИ, которые реально используются в вертикальных коротких видео (Reels / Shorts / TikTok) на 2025–2026 год.

Не общие слова «текст появляется красиво», а конкретные названные приёмы с описанием механики: что именно движется, за сколько кадров, с каким замедлением. Примеры того, что меня интересует: karaoke-подсветка слова, word-pop с перелётом, маска-шторка по строке, смена гарнитуры в ритм (font-cut), текст-стенсиль сквозь который видно видео, текст по траектории, разлёт по буквам, эффект печатной машинки с курсором, текст с параллаксом за головой говорящего (text-behind-subject).

Для каждого приёма: название, что делает, когда уместен, как выглядит механика (какие свойства анимируются), сложность реализации в React/Remotion.` },

  { key: 'infographic', prompt: `Собери приёмы АНИМИРОВАННОЙ ИНФОГРАФИКИ поверх говорящей головы в вертикальных роликах (подкаст-клипы, экспертный контент) на 2025–2026.

Меня интересуют способы ПОКАЗАТЬ структуру мысли: воронка, которая наполняется сверху вниз; пирамида, которая строится по уровням; цепочка шагов; дерево решений; сравнение «было / стало»; счётчик цифры; шкала-прогресс; чек-лист с галочками; диаграмма из блоков; карта связей; таймлайн; стопка карточек; круговой прогресс.

Для каждого: название, какую МЫСЛЬ он визуализирует (это главное — под какой смысл он нужен), механика появления, типичная длительность, ошибки при использовании.

Отдельно: приёмы работы с фоном — затемнение, виньетка, размытие, дуотон, чтобы вставка читалась поверх видео.` },

  { key: 'editing', prompt: `Собери приёмы МОНТАЖА, которыми топовые подкаст-клипы и экспертные Reels удерживают внимание на 2025–2026.

Интересует: ритм нарезки (как часто менять картинку), джамп-каты и как их прятать, зумы (punch-in) и когда они уместны, спид-рампы, b-roll и правила его вставки, скриншоты и как их подавать, split-screen, whip-pan переходы, glitch/RGB-split на акцентах, звуковые акценты (whoosh, риверс, тишина перед фразой), тряска камеры на ударе, вспышка кадра.

Для каждого: когда применять, какая длительность, чем портят ролик при злоупотреблении. Отдельно — что из этого создаёт ощущение ДОРОГОГО монтажа, а что выглядит дёшево и устарело в 2026.` },

  { key: 'code', prompt: `Найди РЕАЛЬНО СУЩЕСТВУЮЩИЕ открытые библиотеки, репозитории и наборы компонентов для анимаций в Remotion (React-видео) и для кинетических субтитров.

Проверяй по GitHub и npm — не по сайтам-каталогам AI-инструментов, они выдумывают. Для каждой находки: точное имя репозитория owner/repo, звёзды, дата последнего коммита, лицензия, что конкретно даёт (список компонентов/эффектов), как ставится.

Интересует: @remotion/* официальные пакеты (animation-utils, transitions, shapes, paths, motion-blur, layout-utils, google-fonts), сторонние наборы анимаций, библиотеки кинетических субтитров, генераторы captions, коллекции easing/spring-пресетов, remotion-templates. Также посмотри, есть ли готовые компоненты воронки/пирамиды/таймлайна.

Верни только то, что реально нашёл и проверил.` },

  { key: 'rules', prompt: `Сформулируй ПРАВИЛА ВЫБОРА анимации под смысл фразы для вертикальных экспертных видео.

Задача: построить систему-фильтр вида «если в речи звучит X — уместен приём Y». Нужны конкретные триггеры, которые можно определить по тексту транскрипта.

Проработай как минимум эти классы смысла: называется число или сумма денег; перечисляется последовательность шагов; сравниваются два состояния (было/стало, они/мы); называется проблема или боль; называется результат или выигрыш; звучит противопоставление или отрицание («не X, а Y»); идёт перечисление списком; звучит имя инструмента или бренда; описывается процесс превращения одного в другое; звучит цитата или чужие слова; идёт вывод или мораль; звучит вопрос к зрителю; называется временной период.

Для каждого класса: словесные триггеры в русской речи (конкретные слова и обороты), какой визуальный приём уместен, какой НЕ уместен и почему, сколько секунд держать.

Отдельно: правила ПЛОТНОСТИ — сколько вставок на 30 секунд это норма, а сколько уже мельтешение; и правила приоритета, когда два приёма конкурируют за один момент.` },
]

const FINDINGS = {
  type: 'object',
  required: ['patterns', 'notes'],
  properties: {
    patterns: {
      type: 'array',
      items: {
        type: 'object',
        required: ['name', 'what_it_shows', 'mechanics', 'when', 'avoid_when', 'difficulty'],
        properties: {
          name: { type: 'string', description: 'Короткое имя приёма по-русски + англоязычный термин в скобках' },
          what_it_shows: { type: 'string', description: 'Какую МЫСЛЬ или смысл он визуализирует' },
          mechanics: { type: 'string', description: 'Что именно анимируется: свойства, длительность, характер движения' },
          when: { type: 'string', description: 'В какой момент речи уместен — по возможности словесные триггеры' },
          avoid_when: { type: 'string', description: 'Когда НЕ применять и чем портит' },
          difficulty: { type: 'string', enum: ['низкая', 'средняя', 'высокая'] },
          duration_sec: { type: 'string' },
          source: { type: 'string', description: 'Откуда взято — ссылка или репозиторий; «практика жанра», если общее место' },
        },
      },
    },
    notes: { type: 'string', description: 'Выводы, которые не уложились в таблицу: тренды 2026, что устарело, риски' },
  },
}

const VERDICT = {
  type: 'object',
  required: ['kept', 'dropped', 'why'],
  properties: {
    kept: { type: 'array', items: { type: 'string' }, description: 'Имена приёмов, которые прошли проверку' },
    dropped: { type: 'array', items: { type: 'string' }, description: 'Имена приёмов, которые надо выбросить' },
    why: { type: 'string', description: 'По каждому выброшенному — причина: выдумка, устарело, невозможно в Remotion, дублирует другой' },
  },
}

const results = await pipeline(
  LENSES,
  (l) => agent(
    `${l.prompt}

КОНТЕКСТ ЗАКАЗЧИКА. Александр — владелец онлайн-школы про ИИ (Казахстан), снимает вертикальные рилсы из записей подкастов: говорящая голова крупным планом, пословные субтитры внизу, заголовок сверху. Бренд: чёрный фон, лайм #B6FF00, оранж #FC5C02, шрифт Benzin. Реализация — React через Remotion, слой с прозрачным фоном рендерится кадрами PNG и накладывается на видео через ffmpeg.

Он сказал дословно: «плашки в плашки выкатываются» — это ему уже наскучило. Просит: «затемнил фон, и раз — хуяк, одна плашка появляется вверху, и по пирамиде уже, как воронка: сначала сделал это, потом это — и так воронка, бам-бам, наполняется».

Значит нужны приёмы, которые ПОКАЗЫВАЮТ СТРУКТУРУ МЫСЛИ, а не просто выводят текст.

ТРЕБОВАНИЯ К ОТВЕТУ:
- Только то, что реально применимо в вертикали 1080×1920 поверх говорящей головы.
- Никаких выдуманных названий ради красоты — если приём общеизвестен, так и пометь.
- Механику описывай так, чтобы по ней можно было написать React-компонент.
- Не предлагай то, что требует 3D-движка, съёмки с хромакеем или ручной ротоскопии.

Следуй Karpathy-guidelines: не выдумывай, при неопределённости говори прямо.`,
    { label: `разведка:${l.key}`, phase: 'Разведка', schema: FINDINGS }
  ),
  (res, l) => {
    if (!res) return null
    const names = (res.patterns || []).map((p) => `${p.name} — ${p.what_it_shows} — ${p.mechanics}`).join('\n')
    return agent(
      `Ты скептик-практик. Другой агент собрал приёмы анимации по теме «${l.key}»:

${names}

ЗАДАЧА: отсеять то, что не выдержит реальности. Выбрасывай приём, если верно хотя бы одно:
1. Выдуманное название или приём, которого в жанре не существует — проверь, гуглится ли термин.
2. Требует того, чего у нас нет: 3D-движка, хромакея, ротоскопии, сегментации человека по маске в реальном времени, платных плагинов.
3. Невозможно или мучительно реализовать React-компонентом в Remotion за разумное время.
4. Визуально не читается в вертикали 1080×1920 поверх лица крупным планом — например, требует много мелкого текста или широкой горизонтальной раскладки.
5. Дублирует другой приём из этого же списка без содержательной разницы.
6. Выглядит устаревшим на 2026 год.

Отдельно проверь честность: если приём заявлен как «тренд 2026», найди подтверждение, иначе понижай до «практика жанра».

По умолчанию сомневаешься — выбрасывай. Лучше десять рабочих приёмов, чем тридцать красивых на бумаге.`,
      { label: `проверка:${l.key}`, phase: 'Проверка', schema: VERDICT }
    ).then((v) => ({ lens: l.key, findings: res, verdict: v }))
  }
)

const clean = results.filter(Boolean)
const survived = []
for (const r of clean) {
  const drop = new Set((r.verdict?.dropped || []).map((s) => s.toLowerCase().trim()))
  for (const p of r.findings.patterns || []) {
    const isDropped = [...drop].some((d) => p.name.toLowerCase().includes(d) || d.includes(p.name.toLowerCase().split('(')[0].trim()))
    if (!isDropped) survived.push({ lens: r.lens, ...p })
  }
}
log(`Приёмов выжило: ${survived.length} из ${clean.reduce((n, r) => n + (r.findings.patterns || []).length, 0)}`)
return {
  survived,
  notes: clean.map((r) => ({ lens: r.lens, notes: r.findings.notes, dropped: r.verdict?.dropped, why: r.verdict?.why })),
}
